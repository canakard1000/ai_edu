import { defineConfig } from '@apps-in-toss/web-framework/config';

export default defineConfig({
  appName: 'ttakmoye',
  brand: { primaryColor: '#1e6c47' },
  permissions: [],
  webBundleDir: 'dist'
});
